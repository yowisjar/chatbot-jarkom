# Wajib di-set juga di Vercel → Settings → Environment Variables
# URL backend (Ngrok). Boleh dengan atau tanpa /api (otomatis dinormalisasi).
VITE_API_URL=https://untearable-mouill-daniell.ngrok-free.dev/api

# Development lokal (opsional, salin ke .env.local):
# VITE_API_URL=http://localhost:5000/api
